import React,{useEffect,useState} from 'react'; import {createRoot} from 'react-dom/client'; import './style.css';
const API='https://sapyon-assignment.onrender.com';
function App(){const [auth,setAuth]=useState(null),[email,setEmail]=useState('ada@northstar.test'),[password,setPassword]=useState('demo-password'),[projects,setProjects]=useState([]),[selected,setSelected]=useState(null),[tasks,setTasks]=useState([]),[dash,setDash]=useState(null),[detail,setDetail]=useState(null),[note,setNote]=useState(''); const headers=auth?{Authorization:'Bearer '+auth.token,'Content-Type':'application/json'}:{};
 const load=async()=>{let p=await (await fetch(API+'/projects',{headers})).json();setProjects(p); if(p[0]) open(p[0])}; const open=async p=>{setSelected(p);setDetail(null);setTasks(await (await fetch(API+'/projects/'+p.id+'/tasks',{headers})).json());setDash(await (await fetch(API+'/projects/'+p.id+'/dashboard',{headers})).json())};
 const login=async e=>{e.preventDefault();let r=await fetch(API+'/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,password})});if(!r.ok)return alert('Sign-in failed');setAuth(await r.json())}; useEffect(()=>{if(auth)load()},[auth]);
 const show=async id=>setDetail(await (await fetch(API+'/tasks/'+id,{headers})).json()); const comment=async()=>{if(!note)return; await fetch(API+'/tasks/'+detail.id+'/comments',{method:'POST',headers,body:JSON.stringify({body:note,is_client_visible:true})});setNote('');show(detail.id)};
 if(!auth)return <main className="login"><h1>AgencyDesk</h1><p>Agency and client project workspace</p><form onSubmit={login}><input value={email} onChange={e=>setEmail(e.target.value)}/><input type="password" value={password} onChange={e=>setPassword(e.target.value)}/><button>Sign in</button></form><small>Try sam@acme.test for the client portal.</small></main>;
 return <main><header><div><b>AgencyDesk</b><span>{auth.user.name} · {auth.user.email}</span></div><button onClick={()=>setAuth(null)}>Sign out</button></header><div className="layout"><aside><h3>Projects</h3>{projects.map(p=><button className={selected?.id===p.id?'active':''} onClick={()=>open(p)} key={p.id}>{p.name}</button>)}</aside><section>{selected&&<><h1>{selected.name}</h1><p>{selected.description}</p><div className="metrics"><div><b>{dash?.hours_logged}h</b><small>logged</small></div>{Object.entries(dash?.task_counts||{}).map(([k,v])=><div key={k}><b>{v}</b><small>{k.replace('_',' ')}</small></div>)}</div><h2>Tasks</h2><div className="tasks">{tasks.map(t=><button key={t.id} onClick={()=>show(t.id)}><strong>{t.title}</strong><span>{t.status} · {t.priority} · {t.is_client_visible?'Client visible':'Internal'}</span></button>)}</div></>}</section>{detail&&<aside className="detail"><button onClick={()=>setDetail(null)}>Close</button><h2>{detail.title}</h2><p>{detail.description}</p><h3>Comments</h3>{detail.comments.map(c=><p className="comment" key={c.id}>{c.body}</p>)}<textarea value={note} onChange={e=>setNote(e.target.value)} placeholder="Add a comment"/><button onClick={comment}>Comment</button><h3>Files</h3>{detail.attachments.map(a=><p key={a.id}>{a.filename} — {a.approval_status}</p>)}</aside>}</div></main>};createRoot(document.getElementById('root')).render(<App/>);
